DMessage("AV_Int_Base03: start");
main();
DMessage("AV_Int_Base03: end");
function main()
{
    CMessage("Can you even grasp the idea of needing to cum this much");
    setTempVar("AV_IntBase03", true);
    let answer0 = getInput("and knowing you\'ll only be able when I allow you?");
    if (answer0.isLike("yes", "yea", "yep"))
    {
        iLoveTeaseAndDenialResponse();
    }
    else if (answer0.isLike("no", "nope", "nah", "not"))
    {
        aV_NoBadResponse();
    }
    let answer1 = getInput("This is something you\'re just going to have to accept, right?");
    if (answer1.isLike("yes", "yea", "yep"))
    {
        iLoveBeingControlledResponse();
    }
    else if (answer1.isLike("no", "nope", "nah", "not"))
    {
        aV_NoBadResponse();
    }
    return;
}