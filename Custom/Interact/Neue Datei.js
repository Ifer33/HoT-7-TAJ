main();
function main()
{
    CMessage(random("I\'m really being %Mean% to that %Cock% of yours %GeneralTime%, aren\'t I? ", "Ok %PetName% ", "Does your whole body want to cum? ") + random("You ", "We ") + "can bla...for " + random("a while", "now"));
    setTempVar("StopAllowed", true);
    End();
    return;
    CMessage(random("I\'m really being %Mean% to that %Cock% of yours %GeneralTime%, aren\'t I? ", "Ok %PetName% ", "Does your whole body want to cum? ") + random("You ", "We ") + "can stop...for " + random("a while", "now"));
    setTempVar("StopAllowed", true);
    End();
    return;
    CMessage(random("I\'m really being %Mean% to that %Cock% of yours %GeneralTime%, aren\'t I? ", "Ok %PetName% ", "Does your whole body want to cum? ") + random("You ", "We ") + "can bla...for " + random("a while", "now"));
    setTempVar("StopAllowed", true);
    End();
    return;
}