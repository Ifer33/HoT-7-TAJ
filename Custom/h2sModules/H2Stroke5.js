DMessage("H2Stroke5: start");
main();
DMessage("H2Stroke5: end");
function main()
{
    CMessage(random("This is a very special game", "I want you fully focused on this"));
    CMessage(random("You are asking", "what this will be all about?", "I will try to explain to you, what I have in mind."));
    CMessage("Our goal is reach a state of edging, wherein you become completely focused and entranced by the sensation coming from your  %Cock%.");
    CMessage("This state is called gooning.");
    CMessage(random("That\'s the state we are heading for", "While gooning, your erection will be incredibly intense"));
    CMessage(random("I want you to build up your arousal", "so that we can get started with a lot of edging", "We will build it up slowly,", "getting more and more intense"));
    CMessage("And since you already know, what its all about, we can switch over to the next part.");
    CMessage("For now you will just head out for the edges.");
    CMessage("And since we are already talking about it, I want you to hit your first edge");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    CMessage("After hitting an edge you will back off and cool down.");
    //--Command:TagAss
    //--Command:TagCloseUp
	showTaggedImage(4, ["ass","closeup"]);
    CMessage("Better not thinking of my sexy %Ass% while cooling down %EmoteLaugh%");
    sleep(randomInt(10, 30));
    CMessage("But as mentioned before, the amount of edges will increase.");
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("And the breaks between will decrease");
    CMessage("Lets try another one");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    //--Command:TagBoobs
	showTaggedImage(4, ["boobs"]);
    CMessage("Cooling back down a bit.");
    sleep(randomInt(10, 30));
    CMessage("Try to keep your %Cock% always at a maximum of arousal.");
    //--Command:TagBoobs
    //--Command:TagCloseUp
    //--Command:TagNaked
	showTaggedImage(4, ["boobs","closeup","naked"]);
    CMessage("Will my %Boobs% help?");
    sleep(randomInt(10, 30));
    CMessage("But be aware, cause the next edge will follow soon.");
    CMessage("Here we go");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(10, 30));
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("Inhale ...");
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("and exhale ...");
    CMessage("never forget to breathe %SubName%");
    CMessage("Go for the next edge");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(10, 30));
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("Inhale ...");
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("and exhale ...");
    CMessage("Inhale and edge!");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(10, 30));
    CMessage("Intensity is speeding up a bit.");
    //--Command:TagAss
    //--Command:TagCloseUp
    //--Command:TagNaked
	showTaggedImage(4, ["ass","closeup","naked"]);
    CMessage("Did you recognize that? Or are you just thinking of my %Ass%?");
    CMessage("I bet not really, cause the breaks are still long enough.");
    //--Command:TagBoobs
	showTaggedImage(4, ["boobs"]);
    CMessage("And I am sure that you just visualize my %Boobs%");
    CMessage("But this one is definitely too long,");
	DoEdges(1,1,0);
    sleep(randomInt(10, 20));
    CMessage("Really use the breaks between, to calm down as much as possible.");
    CMessage("Otherwise this will going to get really hard to reach the gooning state.");
    CMessage("But I think, we are on a good way");
    DoEdges(1,1,0);
    sleep(randomInt(10, 20));
    CMessage("We are still in phase 1, where you dont hold out these arousing edges.");
    CMessage("Can you imagine what its all about if you reach phase 2?");
    DoEdges(1,1,0);
    sleep(randomInt(10, 20));
    CMessage("How are you doing %SubName%?");
    let answer0 = getInput("Everything ok?");
	while (!(answer0.isLike("yes") || answer0.isLike("no") ))
    {
        answer0 = getInput("%YesOrNo%");
    }
    if (answer0.isLike("no"))
    {
        CMessage("Than take a short break");
        setRapidText(true);
        Break();
        return;
    }
    else if (answer0.isLike("yes"))
    {
        CMessage("Thats fine, so we can move on");
        setRapidText(true);
        Edges_no_hold_2();
        return;
    }
    Break();
}
function Break()
{
    setRapidText(false);
    CMessage("Just take a break without stroking");
    CMessage("Just watch this video and DO NOT TOUCH YOUR %Cock%");
    CMessage("Have some drink in this break, and if necessary lube your %Cock% up again");
    showCategoryVideo(randomCategory(),true);
    Edges_no_hold_2();
}
function Edges_no_hold_2()
{
    setRapidText(false);
    CMessage("Here we go");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(8, 15));
    CMessage("Pretty intense, dont you think?");
    //--Command:TagLegs
    //--Command:TagNaked
	showTaggedImage(4, ["legs","naked"]);
    CMessage("Stay focused, focus on my %Legs%");
    //--Command:TagLegs
    //--Command:TagCloseUp
    //--Command:TagNaked
	showTaggedImage(4, ["legs","closeup","naked"]);
    CMessage("Its hard I can imagine, but thats your goal");
    CMessage("Because the next edges are waiting for you");
    holdEdge();
    sleep(randomInt(8, 15));
    //--Command:TagFace
    //--Command:TagCloseUp
    //--Command:TagSideview
	showTaggedImage(4, ["face","closeup","sideview"]);
    CMessage("OH OH this was a new one " + "-)");
    //--Command:TagFace
    //--Command:TagCloseUp
    //--Command:TagSideview
	showTaggedImage(4, ["face","closeup","sideview"]);
    CMessage("Did you like it?");
    CMessage("Or was it too early for you?");
    CMessage("Ok lets calm down a bit from this one");
    sleep(randomInt(20, 40));
    CMessage("Always keep that goal in mind!");
    CMessage("We are heading towards the gooning state.");
    CMessage("Its also totally arousing for me.");
    //--Command:TagPussy
    //--Command:TagCloseUp
    //--Command:TagNaked
	showTaggedImage(4, ["pussy","closeup","naked"]);
    CMessage("I am also playing with my %Pussy%, while watching you stroke");
    CMessage("Lets try another one");
    DoEdges(1,1,0);
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(8, 15));
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("What else did i tell you before?");
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("Can you still remember?");
    //--Command:TagFace
    //--Command:TagBoobs
    //--Command:TagSideview
	showTaggedImage(4, ["face","boobs","sideview"]);
    CMessage("Or do you start loosing your mind?");
    holdEdge();
    sleep(randomInt(8, 15));
    CMessage("I told you, that the break between the edges will decrease dramatically on our way.");
    //--Command:TagFace
    //--Command:TagSideview
	showTaggedImage(4, ["face","sideview"]);
    CMessage("Know what this means?");
    CMessage("Next edge is on the way");
    holdEdge();
    sleep(randomInt(8, 15));
    CMessage("Oh it was another you needed to hold, what a pity.");
    sleep(randomInt(8, 15));
    CMessage("But thats the name of the game.");
    CMessage("Living on the edge");
    holdEdge();
    sleep(randomInt(8, 15));
    //--Command:TagFace
    //--Command:TagCloseUp
	showTaggedImage(4, ["face","closeup"]);
    CMessage("Can you still handle that?");
    CMessage("Always have in mind, that you dont want to jump over the point-of-no-return.");
    //--Command:TagPussy
    //--Command:TagNaked
    //--Command:TagCloseUp
	showTaggedImage(4, ["pussy","closeup","naked"]);
    CMessage("And of course my dripping %Pussy% %Grin%");
    CMessage("Otherwise the game is over for today!");
    CMessage("But I think you are making good progress.");
    holdEdge();
    sleep(randomInt(8, 15));
    holdEdge();
    sleep(randomInt(8, 15));
    DoEdges(1,1,0);
    sleep(randomInt(8, 15));
    let answer0 = getInput("Hmmm thats hot, isnt it?");
    if (answer0.isLike("yes", "yea", "yep"))
    {
        aV_YesGoodResponse();
    }
    else if (answer0.isLike("no", "nope", "nah", "not"))
    {
        //AV_NoBad();
		aV_NoBadResponse();
        return;
    }
    CMessage("%RelaxAndBreathe% %PetName%");
    return;
}