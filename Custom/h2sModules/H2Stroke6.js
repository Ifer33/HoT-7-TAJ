DMessage("H2Stroke6: start");
main();
DMessage("H2Stroke6: end");
function main()
{
    CMessage(random("This is a very special game", "I want you fully focused on this"));
    CMessage(random("You are asking", "what this will be all about?", "I will try to explain to you, what I have in mind."));
    CMessage("Our goal is reach a state of edging, wherein you become completely focused and entranced by the sensation coming from your  %Cock%.");
    CMessage("This state is called gooning.");
    CMessage(random("That\'s the state we are heading for", "While gooning, your erection will be incredibly intense"));
    CMessage(random("I want you to build up your arousal", "so that we can get started with a lot of edging", "We will build it up slowly,", "getting more and more intense"));
    CMessage("And since you already know, what its all about, we can switch over to the next part.");
    CMessage("You started already holding out these edges for some seconds before backing off and cooling down.");
    CMessage("Now there will be only such edges, so prepare that you will hold ALL of them.");
    CMessage("Lets try this out and bring yourself to the edge");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(20, 30));
    CMessage("How does this feel to you at this state?");
    CMessage("I promise you that there will lots of edges like this following.");
    CMessage("Here is your next one following");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(20, 30));
    CMessage("This one was slightly longer, than the first one I think.");
    CMessage("Holding out these edges isnt that easy, is it?");
    CMessage("Therefor you need to practice.");
    CMessage("Here you go");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(20, 30));
    CMessage("Dont forget to breathe inbetween %PetName%");
    CMessage("And always lube your %Cock%");
    CMessage("%Edge%");
    holdEdge();
    sleep(randomInt(10, 20));
    let answer0 = getInput("Is the time to cool down enough for you?");
    if (answer0.isLike("yes"))
    {
        CMessage("Ok lets keep going.");
        setRapidText(true);
        Edges_hold_2();
        return;
    }
    else if (answer0.isLike("no"))
    {
        CMessage("Then you should again take a break");
        setRapidText(true);
        Break2();
        return;
    }
    Break2();
}
function Break2()
{
    setRapidText(false);
    CMessage("Close your eyes and calm down.");
    CMessage("Take as much time as you need.");
    CMessage("Just take care, that your %Cock% doesnt get soft.");
    CMessage("To keep your brain busy, I will show you one video of your nice collection");
    playVideo("Videos" + java.io.File.separator + "*.*",true);
    let answer0 = getInput("Give me a sign, when you feel ready to go.");
	while (!(answer0.isLike("yes") || answer0.isLike("ready")))
    {
		if (answer0.isLike("no"))
		{
			answer0 = getInput("Take time");
		}else{
			answer0 = getInput("Ready?");
		}
    }
    if (answer0.isLike("ready")||answer0.isLike("yes"))
    {
        CMessage("Me too");
        setRapidText(true);
        Edges_hold_2();
        return;
    }
    else
    {
        CMessage("Thats fine");
    }
    Edges_hold_2();
}
function Edges_hold_2()
{
    setRapidText(false);
    CMessage("Its going to get harder");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(10, 30));
    //--Command:TagFace
	showTaggedImage(4, ["face"]);
    CMessage("What do you think about?");
    CMessage("Are you worried to handle that?");
    holdEdge();
    sleep(randomInt(10, 20));
    CMessage("Just keep your goal in mind!");
    //--Command:TagAss
	showTaggedImage(4, ["ass"]);
    CMessage("Focus on the important things, like my sexy %Ass%");
    holdEdge();
    sleep(randomInt(10, 20));
    //--Command:TagPussy
    //--Command:TagCloseUp
	showTaggedImage(4, ["pussy","closeup"]);
    CMessage("Dont go over the point of no return! Even if you see my %Pussy%");
    //--Command:TagBoobs
	showTaggedImage(4, ["boobs"]);
    CMessage("Vary your stroking speed! Think of my %Boobs% instead %Grin%");
    CMessage("Yes even stop for some seconds if needed");
    holdEdge();
    sleep(randomInt(10, 20));
    CMessage("But as soon as possible, resume stroking");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(10, 20));
    CMessage("We want to reach a special state ...");
    CMessage("Where you are continuously stroking");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(5, 15));
    CMessage("But always stay just under that special point");
    //--Command:TagBoobs
    //--Command:TagCloseUp
	showTaggedImage(4, ["boobs","closeup"]);
    CMessage("Never go over it, even if you see my %Boobs%");
    holdEdge();
    sleep(randomInt(5, 25));
    CMessage("But even master edgers need a break from time to time");
    CMessage("Take some deep breathes");
    sleep(60);
    CMessage("Need to put some more lube on?");
    CMessage("Or drink some water?");
    sleep(randomInt(5, 15));
    CMessage("These are important things %SubName%");
    CMessage("Because the next big wave of edges is coming up on you");
    sleep(3);
    CMessage("And will be very intense");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(5, 10));
    holdEdge();
    sleep(randomInt(5, 10));
    CMessage("Use more lube!");
    sleep(randomInt(5, 10));
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(5, 10));
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(5, 10));
    //--Command:TagBoobs
    //--Command:TagCloseUp
	showTaggedImage(4, ["boobs","closeup"]);
    CMessage("Look my nipples are getting hard");
    holdEdge();
    sleep(randomInt(5, 25));
    CMessage("Oh my god this must be very intense for you.");
    //--Command:TagPussy
    //--Command:TagCloseUp
	showTaggedImage(4, ["pussy","closeup"]);
    CMessage("I am getting soaking wet while watching you %SubName%");
    holdEdge();
    sleep(randomInt(5, 25));
    CMessage("Your %Cock% must be leaking like hell");
    CMessage("And it seems that this is going to get much more intense");
    holdEdge();
    sleep(randomInt(5, 10));
    //--Command:TagAss
    //--Command:TagCloseUp
	showTaggedImage(4, ["ass","closeup"]);
    CMessage("Hope my beautiful %Ass% will not let you go over");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(5, 10));
    CMessage("Oh did I hear you moaning?");
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(5, 10));
    CMessage("Are you entering the state of trance yet?");
    holdEdge();
    sleep(randomInt(5, 10));
    CMessage("Is your mind intimately merging with your %Cock% already?");
    holdEdge();
    sleep(randomInt(5, 10));
    holdEdge();
    //playAudio("Audio" + java.io.File.separator + "tease" + java.io.File.separator + "cEdge.mp3");
    sleep(randomInt(10, 30));
    CMessage("WOW %SubName%");
    CMessage("You were really gooning out babe!");
    CMessage("Simply awesome!");
    let answer0 = getInput("How do you feel now?");
    if (answer0.isLike("good", "fantastic", "great"))
    {
        CMessage("I can imagine that.");
    }
    else if (answer0.isLike("bad", "awful", "shit"))
    {
        CMessage("Oh you didnt like it though?");
    }
    else
    {
        CMessage("I really liked it very, very much!");
    }
    return;
}